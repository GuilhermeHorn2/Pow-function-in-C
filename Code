#include <stdio.h>
#include <stdlib.h>
#include <string.h>

float pow(float num,int exp){
    float value = 1;
    for (int i = 0;i < exp;i++){
        value *= num;
    }
    return value;
}

int main()
{
   

 return 0;
}
